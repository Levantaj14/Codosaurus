import greenfoot.*;  // (World, Actor, GreenfootImage, Greenfoot and MouseInfo)

/**
 * Write a description of class dino_t here.
 * 
 * @author (your name) 
 * @version (a version number or a date)
 */
public class dino_t extends Actor
{
    /**
     * Act - do whatever the dino_t wants to do. This method is called whenever
     * the 'Act' or 'Run' button gets pressed in the environment.
     */
    public dino_t()
    {
        GreenfootImage image = getImage();  
        image.scale(100, 100);
        setImage(image);
    }
    public void act() 
    {
        // Add your action code here.
    }    
}
